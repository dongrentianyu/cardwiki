caption:在rust中，如果访问的索引超出了数组的范围，会发生什么？
created:20230429192839685
creator:马不前
IsOtherText:question
tags:? 
title:在rust中，如果访问的索引超出了数组的范围，会发生什么？

编译时会通过。但也会简单检查一下，如果是很简单的问题，编译就会直接报错。但如果复杂了可能就发现不了。

但运行时绝对会检查panic报错。因为在rust语言中，不允许往后读取内存，也就是不允许占用后续内容。其他语言里可能会允许这样做。



